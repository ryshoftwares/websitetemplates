This variant uses inline SVG icons directly inside the HTML, so no icon files are required here.
Drop any custom SVG/PNG icon assets in this folder if you later want to reference them by file
instead of inline markup.
