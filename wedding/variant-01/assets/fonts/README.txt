Fonts (Playfair Display + Jost) are loaded from Google Fonts via the <link> tags in each HTML
head, so no local font files are required. Add .woff2 files here and update css/style.css with
@font-face rules if you need to self-host the typefaces instead.
