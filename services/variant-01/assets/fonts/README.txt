This template loads its typefaces (Playfair Display + Work Sans) from the Google Fonts CDN via <link> tags in the <head> of every page.
If you need to self-host fonts instead (e.g. for offline use or stricter privacy requirements), download the font files and place them here, then update the @font-face declarations in css/style.css and remove the Google Fonts <link> tags.
