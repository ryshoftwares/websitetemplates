All icons in this template are inline SVG embedded directly in the HTML — there are no icon files to manage.
If you'd rather use icon files (SVG/PNG) instead of inline markup, drop them in this folder and swap the inline <svg> blocks for <img> tags pointing here.
