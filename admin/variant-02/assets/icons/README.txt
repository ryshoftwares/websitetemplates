All icons in this variant (sidebar nav, topbar search/bell/caret, sort arrows,
chart legend swatches, form success check) are written as inline <svg> markup
directly in the HTML — there is no icon font or icon library dependency. This
folder is here for any custom icon files (e.g. a favicon or a brand mark
export) you want to add later.
